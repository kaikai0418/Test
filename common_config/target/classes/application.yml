server:
  port: 3344

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kaikai0418/boot-config.git #GitHub上面的git仓库名字
          username: kaikai0418
          password: kaikai010418